/**
 * Drizzle schema entry point.
 *
 * Intentionally empty at bootstrap — real tables will be added as feature
 * modules (users, games, requests, credits, etc.) come online.
 *
 * Each domain should declare its tables in `src/<module>/<module>.schema.ts`
 * and re-export them here so Drizzle Kit discovers the full schema.
 */

export {};
